#!/bin/sh
# Reject commits containing "Co-Authored-By" in the commit message.

if grep -qi "Co-Authored-By" "$1"; then
  echo "ERROR: Commit message contains 'Co-Authored-By'. Remove it before committing."
  exit 1
fi
